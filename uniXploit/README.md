# uniXploit

An exploit tool with a couple of different target actions. It can grab and decrypt the passwd file, or it can get you a shell. For now. More to come.
params: uniXploit [ip] [target (shell, passwd)]

must compile through GreybelVS for now.